<header>EAT-DA-BORGER</header>
<div class = "separator">
  <h2>Your uneaten burgers!</h2>
  <ul>
    {{#each burgers}}
      {{#unless devoured}}
        {{>burgers/burger-block}}
      {{/unless}}
    {{/each}}
  </ul>
</div>

<div class = "separator">
  <h2>Your devoured burgers!</h2>

  <ul>
    {{#each burgers}}
      {{#if devoured}}
      <li>{{burger_name}}</li>
      {{/if}}
    {{/each}}
  </ul>
</div>

<h2>Add a burger!</h2>

<form class = "create-form">
  <label for = "burgerName">Burger:</label>
  <input type = "text" id = "burgerName" name = "name">
  <button type = "submit">Add burger</button>
</form>


<button class = "delete">Clear your plate</button>